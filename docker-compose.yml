version: '3'
services:
  app:
    image: 'jc21/nginx-proxy-manager:latest'
    restart: unless-stopped
    ports:
      - '80:82'
      - '81:83'
      - '443:444'
    volumes:
      - ./data:/data
      - ./letsencrypt:/etc/letsencrypt
